{
  "schema": 1,
  "description": "Filters the output of a KQL query to only include certain columns",
  "execution_settings": {
    "default": {
      "max_tokens": 2048,
      "temperature": 0.5,
      "top_p": 0.2,
      "presence_penalty": 0.0,
      "frequency_penalty": 0.0
    }
  },
  "input_variables": [
    {
      "name": "columns_to_include",
      "description": "The columns to include in the output KQL query",
      "default": ""
    },
    {
      "name": "kql_query",
      "description": "The KQL to modify if necessary",
      "default": ""
    }
  ]
}